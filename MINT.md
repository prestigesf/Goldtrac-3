# Mint

event → live kid → SHA-256 payload_hash → SMT smt_root → chain_root → ML-DSA-65 sig → one JSONL line

Doors: PrestigeMCP tools/call, or
npx tsx src/goldtrac-cli-mint.ts --app prestige-mcp --payload event.json
